#include "main.h"
/**
 * _abs - return absulute of a number
 *
 * @c: input number
 *
 * Return: absulute number
 */
int _abs(int c)
{
	if (c >= 0)
	{
		return (c);
	}
	else
	{
		return (c * -1);
	}
}
